name: Window 10

on: [push, workflow_dispatch]

jobs:
  build:

    runs-on: windows-latest
    timeout-minutes: 9999

    steps:
    - name: Enable TS
      run: Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server'-name "fDenyTSConnections" -Value 0
    - run: Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
    - run: Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 1
    - run: Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "@cyb3rking" -Force)
    - name: Run Secure TCP Bridge
      run: |
        $port = Get-Random -Minimum 15000 -Maximum 45000
        Invoke-WebRequest -Uri "https://github.com" -OutFile "chisel.zip"
        tar -xf chisel.zip
        Write-Output "=========================================="
        Write-Output "YOUR PORT NUMBER IS: $port"
        Write-Output "=========================================="
        Start-Process .\chisel.exe -ArgumentList "client https://herokuapp.com R:$port:127.0.0.1:3389"
    - name: Keep Alive
      run: |
        while ($true) { Start-Sleep -Seconds 60 }
